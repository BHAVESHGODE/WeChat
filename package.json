{
  "name": "wegift",
  "version": "1.0.0",
  "description": "WeGift - MERN multi-feature platform",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client && pip install -r server/gaana_api/requirements.txt",
    "server": "npm run dev --prefix server",
    "client": "npm start --prefix client",
    "flask": "npm run flask-win --prefix server",
    "dev": "concurrently \"npm run server\" \"npm run client\" \"npm run flask\"",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
